import { BlogOverviewState } from './blog-overview-state';

describe('BlogOverviewState', () => {
  it('should create an instance', () => {
    expect(new BlogOverviewState()).toBeTruthy();
  });
});
